# SB10 first review — S097 (attempt 1/2)

- Review date: 2026-09-10
- Reviewer agent ID: 14a122b0-b7d7-45fc-9d64-f63a39703ecc
- Chapter: `batches/SB10/S097.md` — Stage 97/200, "Social-media sentiment composite"
- Plot script: `batches/SB10/plot_S097.py` → `images/S097_example.png` (187,580 B)
- **Verdict: FAILED V4, H2, V2** — the Mermaid diagram does not follow the mandatory 6-node skeleton; the batch's known full-precision values are rounded away; the twin-axes chart is not justified in chapter text. Returned for rework attempt 2/2.

## 22-item QC (merge-protocol §4)

**H — honesty**
- H1: Exact headings S1–S12; "Stage 97/200" exact. ✓
- H2: **FAIL (precision)** — the batch's known items require `+0.0637` and `HHI=0.3168` to appear. The chapter shows `+0.064` and `0.317` (S4 table, hand-checks, script arrays `wtd_sent[6]=0.064`, `hhi[6]=0.317`, and the chart annotation "day 7: HHI 0.317"). Rounding drops the mandated precision. Fix: show +0.0637 / 0.3168 at full precision in the S4 hand-check, and update the script arrays + annotation to the full-precision values, then re-run the script.
- H3: Composite construction (volume z × weighted sentiment, HHI veto) correct. ✓
- H4: Provenance **[SR]** present. ✓
- H5: Explicit cost stack: "$50,000 in a $40 stock = 1,250 shares. Half-spread both ways at 2¢ = $50. Fees $0.0035/share × 2 legs = $8.75. Impact/slippage 3 bps = $15. Total ≈ **$74 ≈ 15 bps round trip**." ✓
- H6: Bot/manipulation caveats explicit; synthetic labels throughout. ✓

**S — structure**: S1–S4 complete; S4 10-day synthetic social tape (seed 97). ✓

**C — cross-refs & citations**
- C1: S032, S042, S093, S097, S099, T048, T071, T072, T080 all exist in `notes/plan.md`; relationships inspected. ✓
- C2: No phantom consumers. ✓
- C3: 9 sources. Spot-checks: (1) Antweiler & Frank (2004), "Is All That Talk Just Noise?", *Journal of Finance* 59(3), 1259–1294 — confirmed; the 1.5M-message / bullishness-index / agreement-index record matches the chapter's usage. (2) Chen, De, Hu & Hwang (2014), "Wisdom of Crowds," *Review of Financial Studies* 27(5), 1367–1403 — confirmed. ✓
- C4: FinBERT ("a FinBERT-class neural language model"), HHI ("Herfindahl–Hirschman index … the sum of squared post shares across accounts"), lexicon, transformer all defined at first use. ✓ Typo: line 85 reads `plan.md \u00a73` (literal backslash-u escape) → must be `plan.md §3`.

**V — visuals**
- V1: House-style block verbatim-identical to spec; watermark + corner caption present. ✓
- V2: **FAIL (minor)** — the chart uses twin axes; the visual spec allows dual axes only when the chapter justifies them. The justification exists only in the script's corner caption ("twin axes justified: left = counts/index (0-1), right = unitless z / sentiment"), not in chapter text. Add one sentence in S5 (e.g., "Twin axes are used because the left panel mixes counts/index (0–1) with unitless z-scores and sentiment — different units that cannot share an axis."). Script exit 0; z-axis reaches 30 so the day-6 value 26.25 is visible, not clipped. ✓ otherwise.
- V3: Chart annotations match S4 (day-7 HHI 0.317, raw +0.45 → weighted +0.06) — subject to the H2 precision fix. ✓
- V4: **FAIL** — the diagram uses a source/ingest/clean/feature/predict/execute/context/risk layout, not the mandatory 6-node skeleton (feed → ingest → feature → signal → gate → out/drop) required by `notes/visual-spec.md` line 109. Edges do carry granularity, but the skeleton is wrong. Redraw: FEED["X/Reddit/StockTwits firehose"] → ING["ingest + normalize"] → FEAT["volume z, weighted sentiment, HHI"] → SIG["Signal S097"] → GATE["manipulation veto / cost gate?"] → OUT/DROP, with per-edge granularity ("(per post)", "(daily panel)", "(per signal)").

## Required fixes (exact)
1. S11: redraw the Mermaid diagram in the 6-node V2-S skeleton with granularity on every data edge (see above).
2. S4 + `plot_S097.py`: use full precision `+0.0637` and `HHI=0.3168` (update script arrays, annotation text, and chapter hand-checks); re-run the script.
3. Line 85: `plan.md \u00a73` → `plan.md §3`.
4. S5: add the twin-axes justification sentence (V2).

## Caveats
- Source-log wording "Grok answers pending" — not failed per task instructions.
