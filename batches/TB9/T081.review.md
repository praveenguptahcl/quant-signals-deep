Reviewer: subagent-1acff360 (TB9 strict reviewer) | Date: 2026-09-10 | Chapter: T081
QC: H1-H6 PARTIAL FAIL (H4, V3) | S1-S4 PASS | C1-C4 PASS | V1,V2,V4 PASS, V3 FAIL
Attempt: 1/2

Plot re-run: `python3 batches/TB9/plot_T081.py` exits 0; `images/T081_example.png` regenerated fresh.
Script output: 21,000 trades → 71 bars (1 dollar, 65 volume, 5 DIB); minute-bar kurtosis 8.4 vs volume-bar kurtosis 4.1. Matches chapter's headline counts.

Spot-checks:
- Citations resolved: doi:10.3905/jpm.2012.39.1.019 opened — title "The Volume Clock: Insights into the High-Frequency Paradigm", J. Portfolio Management 39(1) ✓ matches T10 source 1. arXiv:1011.6402 title-confirmed in-session per writer flag ✓.
- Chart points: bar counts per regime in chart panel 2 (R1 quiet→dollar 1 bar; R2 busy 2-sided→volume 65 bars; R3 informed→DIB 5 bars) vs T4 table — MISMATCH (see V3).

Verdict: FAILED | Issues:

1. V3 FAIL — chart↔text disagreement (hard gate). The script (and the regenerated chart's
   bottom panel) assigns regimes→clocks as: R1 quiet → dollar bars (1 bar), R2 busy 2-sided →
   volume bars (65 bars), R3 informed → DIB/imbalance bars (5 bars) — see plot_T081.py lines 41–49
   and the chart's `ax2` bar labels. The T4 regime table (T081.md lines 67–69) assigns instead:
   R1 quiet → "volume | bulk of 65", R2 high-volume chop → "volume | —", R3 trending →
   "dollar + DIB | 1 + 5". The per-regime clock assignments contradict the plotted data on all
   three rows (totals 1/65/5 agree, the regime mapping does not).
   Minimal fix: rewrite T4 lines 63–69 to the script's mapping — R1 quiet (σ-ratio 0.583,
   |ρ|=0.10) → dollar bars, 1 bar; R2 busy 2-sided (λ 3.25×, |ρ|=0.08) → volume bars, 65 bars;
   R3 informed (|ρ|=0.52) → DIB bars, 5 bars.

2. H4 FAIL — rule-vs-example inconsistency. T2 defines the clock toggle by Hurst/variance-ratio
   (H > 0.55 → dollar; H < 0.45 → DIB; else volume). The worked example's clock selection in
   plot_T081.py uses a different rule entirely: sigma20/median < 0.6× → dollar, lam/median > 3×
   → volume, |rho| > 0.35 → DIB (script lines 44–47, "why" fields). The T4 narrative (line 63)
   "a persistent drift with H ≈ 0.68" is unsupported — no H is computed in the script (R1 has
   |ρ|=0.10, i.e. low persistence). The example does not apply the stated rule.
   Minimal fix: in T4, replace the H≈0.68 sentence with the script's actual conditioning rule
   and add one sentence stating the example's σ/λ/|ρ| triggers are synthetic stand-ins for the
   H-toggle of T2 (or: change the script to implement the H-toggle — writer's choice, but text
   and script must implement the same rule).

3. Advisory (not a fail): "heteroskedastic" (T2) is not defined for a non-quant reader; add a
   5-word gloss. Cost-model citation "(see notes/cost-model.md)" lacks the §2/§3/§5 section
   ref required by S4 — numbers trace to §2 (100–500k events/sec), so this passes but add the
   section number on rework.

Passing notes: H1/H2/H3/H5/H6 PASS (synthetic labeled everywhere incl. exact watermark caption
in T9; no positions so no cost ledger needed; provenance [D/SR] present; SIP/no-colo feasibility
stated honestly in T2). S1–S4 PASS (3 sources; vendor prices carry the exact
`indicative — verify before budgeting` suffix; Grok draft errors disclosed in Unverified leads,
not in evidence). C1–C4 PASS (T1–T10 in order; all S-refs S083/S090/S067 resolve per plan.md §3
TB9 row; heading/stage format correct). V1/V2/V4 PASS (PNG fresh, relative link, descriptive alt
text, house style block, watermark; mermaid fenced, V2-T skeleton, edges quoted, data edges
granularity-labeled e.g. "5-min returns, rolling", "trade prints, tick-level").
