# Review — S088: Purged / embargoed cross-validation

Reviewer: 87f59f01-a967-4268-8e1c-a72e5f3973c5 | Date: 2026-09-10 | Chapter: S088

QC: H1 PASS, H2 PASS, H3 PASS, H4 PASS, H5 PASS, H6 PASS | S1 PASS, S2 FAIL, S3 PASS, S4 PASS | C1 PASS, C2 PASS, C3 PASS, C4 FAIL | V1 PASS, V2 PASS, V3 PASS, V4 FAIL

Spot-checks:
- Citations resolved (2): Bailey, Borwein, López de Prado & Zhu (2017), "The Probability of Backtest Overfitting," *J. Computational Finance* 20(4) — CSCV/PBO framework confirmed. rchhabra17, "probability-of-backtest-overfitting" (https://github.com/rchhabra17/probability-of-backtest-overfitting/blob/HEAD/README.md) — resolves; confirms the CSCV PBO complexity ladder on SPY 2010–2026, S=16 / 12,870 combinations, and PBO 0.37–0.57 non-monotonic in parameter count — matching the chapter's S9 characterization (the chapter correctly reports the project's own refutation of "PBO rises with search-space size").
- Chart points matched (2): 35 train + 17 test + 8 removed = 60 events matches the plotted fold partition; 5 purged + 3 embargoed events match the plotted removed-event markers (plotted under the explicitly flagged chatbot boundary convention, distinguished from de Prado's standard in S3).
- Plot rerun from repo root: `images/S088_example.png`, 134,727 bytes (>20 KB), style block + seed + chapter-ID title in script, `SYNTHETIC EXAMPLE` watermark code present. Seed stated in S4 text.

Structure/provenance: 12 S-sections in exact order; one fenced Mermaid diagram; source-log sentence present; provenance [D]. ≥2 T-strategy references resolve against notes/plan.md §3. H5 passes as n/a (validation procedure; no trades; S9 explicitly labels n/a).

Issues:
1. **S2 FAIL** — the S9 "documented evidence" table contains a row "Chatbot lead Q-SB3-1 (Duck.ai…)" presenting the chatbot's 60-event schedule as evidence. Chatbot-only claims belong in `Unverified leads` per merge-protocol §4 S2.
2. **C4 FAIL** — `PBO` is never expanded as "Probability of Backtest Overfitting (PBO)" in body text (appears only in citation titles); `CSCV` ("CSCV S=16," S9) is never expanded as Combinatorially Symmetric Cross-Validation.
3. **V4 FAIL** — none of the 4 data-carrying Mermaid edges names data granularity ("event table," "timed events," "fold masks," "CV score").
4. **Code defects (non-code, material)** — the S4 ingest sketch contradicts the chapter's own S3 math: (a) S3 defines the test fold as the interval [T_a, T_b] and purges training labels intersecting it, but the code takes `ta, tb` from `.row(0)` — only the *first* test event — so purge misses overlaps with the rest of the test fold; (b) the embargo `.sort("t0").slice(EMB, None)` removes the first EMB rows of the *training* set regardless of fold position, not the EMB observations at the test-fold boundary under either the chatbot convention (first test events) or de Prado's standard (last train observations before the test fold) documented in the S3 convention flag; (c) modulo fold assignment `(k % K)` interleaves rather than blocking folds.
5. **Markdown defect (non-code)** — S9: the body sentence "…showed none." runs directly into the evidence-table header (`| Study / source | …`) with no blank line, so the table does not render as a table.

Verdict: **FAILED** — S2, C4, V4, ingest-code bugs, S9 markdown defect (attempt 1/2)
