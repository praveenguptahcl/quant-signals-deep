# Re-review — S050 (attempt 2/2, FINAL)

Reviewer: acd4b0b4-a6c4-4703-b95b-f5d54dfb14ef | Date: 2026-09-10 | Chapter: S050
QC: H1-H6 PASS | S1-S4 PASS | C1-C3 PASS | C4 FAIL | V1-V4 PASS
Spot-checks: Krauss (2017) J. Economic Surveys 31(2), 513–545 resolves (RePEc/EconPapers); Vidyamurthy (2004) *Pairs Trading: Quantitative Methods and Analysis*, Wiley, resolves (cited bibliography); chart points β̂=2.0301, α̂=−3.32, full-sample day-15 z=−1.5411, gross P&L +2.00 all match `plot_S050.py` output; seed 50 stated in S4 text.
Verdict: QUARANTINED | Issues: C4 — `OHLCV`, `SIP`, `OPRA` used with no in-chapter expansion.

## Rework claims (attempt-2 brief) — verified
- No T100 consumer claim remains; T007/T031 references resolve and are consistent.
- β̂=2.0301, α̂=−3.32, day-15 z=−1.5411, gross P&L +2.00 — all match script output exactly.
- Duplicate S4 table defect gone; explicit cost decomposition present (spread/fees/impact/slippage).

## Full 22-check QC (merge-protocol §4)
- H1 PASS — synthetic labeling in text; `SYNTHETIC EXAMPLE` watermark in script.
- H2 PASS — no real-performance claims; the ~50.5%/Sharpe-8.14 chatbot figure is quarantined in Unverified leads, never a base case.
- H3 PASS — provenance tag `[D]` present.
- H4 PASS — thresholds marked `example` where non-standard.
- H5 PASS — explicit cost decomposition with before/after-cost labeling.
- H6 PASS — no latency-sensitive SIP/L1 claims requiring the label.
- S1 PASS — ≥3 checkable sources; 2 spot-checked resolve (above).
- S2 PASS — no invented IDs/URLs/statistics; chatbot-only claims in Unverified leads.
- S3 PASS — `indicative — verify before budgeting` suffixes present.
- S4 PASS — cost-model cites present (§2/§3/§5).
- C1 PASS — 12 sections in order. C2 PASS — T007/T031 resolve in plan.md §3.
- C3 PASS — heading/stage format correct.
- C4 FAIL — `OHLCV` ("Daily OHLCV", S8) never expanded; `SIP` ("1-min SIP bars", S8) never expanded; `OPRA` ("full OPRA", S7) never expanded. A smart non-quant cannot parse any of the three.
- V1 PASS — `images/S050_example.png` (164,370 bytes), relative ref, descriptive alt.
- V2 PASS — watermark visible; house style; title carries chapter ID.
- V3 PASS — ≥2 chart points match script; seed 50 stated in S4 text.
- V4 PASS — mermaid fenced, skeleton, labeled edges.

## Plot re-run
`python3 batches/SB3/plot_S050.py` → exit 0; PNG 164,370 bytes, byte-identical to prior run; `np.random.default_rng(50)` deterministic.

## Precise fix
1. First `OHLCV` use → `OHLCV (open/high/low/close/volume)`.
2. First `SIP` use → `SIP (Securities Information Processor, the consolidated tape)`.
3. First `OPRA` use → `OPRA (Options Price Reporting Authority)`.

## Non-blocking observations
- Same S4/S10 wording note as S049 (stack lives in S4, not S10) — editorial, not failed.
- `M&A` unexpanded — common business vocabulary, not failed.
