---
id: R050
kind: regime
version: 1.0.0
template_version: 1.0.0
status: draft
batch: RB5
family: regimes
provenance_tier: D
sources:
  - CTA consolidated SIP specification (identifier — the public SIP feed)
unverified_sources:
  - Direct-feed vs SIP latency divergence magnitudes — no verifiable source on this stack; not claimed
consumes: []
regime_ids: []
correlates_with:
  - "T: latency-sensitive intraday (would be gated by this regime if measurable)"
shares_data_feed_with:
  - "R038 (thin liquidity — same bar stack, SIP-only)"
seed: 42050
rng: {algorithm: PCG64, lib_pin: "numpy==2.1.0"}
plot_script: scripts/plot_R050.py
test_cmd: "python3 -m pytest modules/tests/test_R050.py -q"
datasets:
  - name: Direct-feed vs SIP comparison
    vendor: n/a — not available on this stack
    product: requires colocated direct feeds
    schema_version: 1
    fingerprint: "sha256:REPLACE_ON_INGEST"
    vintage: 2026-09-09
    rights: not available
changelog:
  - "1.0.0 2026-09-10: initial module per template v1.0.0"
---

# R050 — SIP-vs-direct divergence (capability-gated)

**THIN / capability-gated:** SIP-vs-direct-feed divergence cannot be measured on a non-colocated, SIP-only stack — the module is specified but permanently `UNKNOWN` here. Provenance **[D]**.

> Tag law: every numeric literal in prose carries one of `[documented]
> [default] [example] [unverified] [internal-est] [measured]` (legend: Appendix
> i v1.0.0). Constants inside cited indicator formulas inherit the formula's
> tag. Bibliographic years, volumes, pages, regulation numbers, section
> numbers, fail-safe codes (F1-F5), module IDs, clock times, and machine-parsed
> `§0` data fields are identifiers, not literals, and are exempt.

## §1 Five-minute reviewer card

| Row | Value |
|---|---|
| Module | R050 — SIP-vs-direct divergence (capability-gated), v1.0.0 |
| Edge verdict | `PREDICTIVE-FEATURE-ONLY` — documented conditioning power over S/T efficacy; no standalone after-cost edge claimed |
| After-cost | `BEFORE-COST` — a regime is a conditioner, not a traded signal; one-sentence verdict: unmeasurable on this stack — no efficacy claim is made; the module exists so the gap is explicit, not silent. |
| Build cost | Not buildable on this stack — capability-gated [internal-est] |
| Data cost | n/a — capability-gated |
| latency_class | intraday (would-be) |
| risk_class | low (regime-only; emits UNKNOWN) |
| Capacity | not applicable — the module emits state only; capacity is set by the gated S/T modules |
| Executability | Not feasible here: measuring SIP-vs-direct divergence requires colocated direct feeds; a Mac/SIP-only stack cannot observe the divergence it would measure |
| Risk summary | The failure mode is pretending to measure it — any SIP-only 'divergence' number is fabrication |
| When it dies | n/a — already gated |
| Regime gates | Would compound with R038 if measurable: divergence + thin = toxic |
| Emits | `regime_state_vector` (Appendix b v1.0.0 adapted) `{regime_id, state, value, estimator_version, data_vintage, computed_at}` — never orders |

## §1.5 Cheapest / least-build path

1. **Prototype hour band:** not buildable on this stack [internal-est] — colocated direct feeds required; SIP-only stack cannot measure this.
2. **Cheapest-source row:** min_tier = colocated direct feeds (not available) — cheapest data source
   candidate [internal-est]; latency_class = intraday (would-be);
   required_fields = direct-feed quotes, SIP quotes, synchronized clocks; price_band = n/a — capability-gated
   [unverified] (indicative — verify before budgeting); build_or_buy =
   **neither — gated on colocation**.
3. **Resource budget:** n/a [internal-est]; compute pattern
   `per_bar`.
4. **Skip list:** everything — until colocation exists.
5. **DO-NOT-CUT list:** causality assertions (`assert fill_event > signal_event`, no-lookahead
   test); COST block + callable `expected_cost_bps`; F1-F5 fail-safes +
   module-state enum; decision-log audit records; provenance tags on every
   numeric literal; fixtures + `test_cmd`; secrets via env only.
6. **Escalation gate:** upgrade from cheapest when any holds — colocated direct-feed infrastructure.

## §R0 Module contract

### R0.1 Typed signature

```python
def detect(state: RegimeModuleState, events: list[Event], cfg: Config) -> RegimeState:
```

`RegimeState` = `{regime_id, state, value, estimator_version, data_vintage,
computed_at, module_state, min_lag_bars}` — the module's `regime_state_vector`
(§1). `state` is the module-owned named state vector (`div_bps`, `module_state`);
`events` are canonical Events (Appendix a v1.0.0), ascending `event_ts`. The
function handles documented edge cases: empty events → `UNKNOWN`;
out-of-bounds indicator values → `UNKNOWN` (F2, never interpolate); halt/auction
events → freeze per the market-state table. It emits regime labels and
cost-interface adjustments only — never orders, never signals.

### R0.2 Config dataclass

| Parameter | Type | Default | Range | Status |
|---|---|---|---|---|
| `min_lag_bars` | int | `1` | `1–5` | default |

All defaults tagged per the tag law (status `default` ⇒ `[default]`).

### R0.3 Canonical schema mapping

Vendor columns → canonical Event schema (Appendix a v1.0.0), stated once here:

| Direct feed (`ts`, bid/ask per venue) | unavailable on this stack |
| SIP NBBO (`ts`, nbbo) | available — insufficient alone |

### R0.4 Time contract

> Time contract: clock domain UTC, int64 ns; authoritative causality clock =
> `event_ts`; max_skew = `1` ms [default]; bar-label = open time; staleness TTL
> = `3`× cadence [default] (F4); gap policy = mask, no interpolation; halt →
> freeze and emit `UNKNOWN`.

**Timing box:** `detect@t (per_bar, America/New_York) → regime label gates signals only for trades @open(t+1) or later`

### Lag contract

- The indicator is causal: the value/state published for time `t` is computed
  using only inputs with timestamps `≤ t` (Appendix e v1.0.0).
- A regime label finalized at `t` may gate signals only for trades at `t+1`
  or later — `earliest fill @open(t+1)` per the bar-label rule.
- Backtests: regime-conditioned variants lag the label by `≥ min_lag_bars` =
  `1` bar(s) [default]; no ex-post period selection (F5).
- No-lookahead check: for every prefix of the input series, the label at `t`
  is identical with and without later data (enforced in `test_no_lookahead`).

### R0.5 Market-state table

| State | Module behavior |
|---|---|
| CONTINUOUS_TRADING | Compute normally; emit per cadence |
| HALTED | Freeze state; emit `UNKNOWN`; discard contributions across reopen |
| AUCTION | No new labels; hold last state; `DEGRADED` |
| CLOSED | Emit nothing; state `OFF` |

### R0.6 Module-state enum + error taxonomy

States per Appendix k v1.0.0: `OK | DEGRADED | UNKNOWN | OFF`. Invalid input →
`UNKNOWN`, never interpolate (Appendix f v1.0.0, F1–F5).

| Failure | State | Alert |
|---|---|---|
| No direct feeds on this stack (F1) | `UNKNOWN` | log |
| Any computed value (F2) | `UNKNOWN` | page |
| Kill/administrative disable | `OFF` | page |
| Anything unmapped | `UNKNOWN` | page |

### R0.7 Ops tail

- **Schedule:** n/a — capability-gated; owner: regime-plant on-call.
- **Health checks:** fixture replay (`test_cmd`) green on deploy; dual-estimator
  agreement monitor (F3); staleness watchdog at `3`× cadence (F4).
- **Alert table:** page on `UNKNOWN` > `30` s [default] or bounds violation;
  notify on `DEGRADED`; log on single dropped inputs.
- **Resource budget:** n/a [internal-est].
- **Runbook stub:** `1.` confirm feed; `2.` replay fixture; `3.` if red → set
  `OFF`, page; `4.` reconcile decision log before re-arm.

### R0.8 Secrets (env-only)

(none) via environment only. No secrets in this file, fixtures, or
logs (CI secrets-grep).

### R0.9 May / must-NOT

> **May:** emit regime labels (`RegimeState`) and cost-interface adjustments
> per the Cost interface block. **Must-NOT:** emit orders, order intents,
> signals, prices, or notionals; select backtest periods ex post (F5);
> interpolate across gaps; call a broker; mutate shared state outside `state`.

### R0.10 Cost-interface hook

The typed adjustment record lives in the **Cost interface** block below; it is
the only channel by which this regime touches cost inputs.

## §R1 Legacy (subsumed by §1)

Legacy §R1 content is the §1 reviewer card above; this header is retained so
section numbering stays frozen.

## §R2 Precise definition

**Capability-gated module.** SIP-vs-direct divergence (the latency/quote gap between the consolidated SIP and colocated direct feeds) is a real regime for latency-sensitive strategies — but it cannot be measured without colocated direct feeds. On this stack (non-colocated, SIP-only), the module is specified and tested but permanently emits `UNKNOWN`. Any SIP-only 'divergence' number would be fabrication; the honest output is `UNKNOWN` with the reason attached.

## §R3 Exact calculation

What would be measured (not measurable here):

$$D_t = \frac{|P^{direct}_t - P^{SIP}_t|}{P^{SIP}_t} \times 10^4 \text{ bps}$$

with synchronized clocks and colocated direct feeds. On this stack both inputs
are unavailable, so `detect` returns `NaN` → `UNKNOWN` by construction (F1/F2).
The fixture is a one-row stub documenting the gate; the acceptance test asserts
`UNKNOWN`, never a fabricated value.

*Formula tag: [example] — constants inside this formula inherit it per the tag law
(Appendix i v1.0.0); worked-check numbers carry their own tags.*

**Data inputs.** Direct-feed quotes and SIP quotes with synchronized clocks — unavailable on this stack.

**Cadence.** Would-be intraday; n/a here.

**Threshold bands.**

| `D` | State | Tag |
|---|---|---|
| measurable | bands would be calibrated per venue | [example] |
| this stack | `UNKNOWN` — capability-gated | [documented] |

Recalibrate every `[example]`-tagged band per instrument before production use.

## §R4 Efficacy modulation

Direction ∈ `amplifies | degrades | inverts`; mechanism states *why*.

| Affected S/T families | Direction | Mechanism |
|---|---|---|
| T: latency-sensitive intraday | would gate | if measurable, divergence would gate latency-sensitive strategies — unmeasurable here, so no claim |

## §R5 Loop spec

| Role | Responsibility |
|---|---|
| Sentinel | would compare direct vs SIP quotes — no direct feeds here |
| Verifier | n/a — nothing to verify against |
| Grok-Researcher | tracks feed-latency literature — leads only |
| Adversary | offers a SIP-only 'divergence' proxy; the gate must reject it as fabrication |
| Gate | accepts measurement only with colocated direct feeds |

**F1–F5 instantiated for this regime.**

- **F1.** no direct feeds → `UNKNOWN` (the honest state)
- **F2.** any computed value → `UNKNOWN` + page (fabrication guard)
- **F3.** n/a — no second estimator exists without direct feeds
- **F4.** n/a — no live feed to go stale
- **F5.** no ex-post anything — the module measures nothing here

## Cost interface: regime state → cost-function adjustment

The regime state maps to an adjustment record applied to the strategy's
`expected_cost_bps(notional, adv_pct, venue, side, urgency)` inputs. The record
is the *only* cost channel (no cost numbers are restated elsewhere).

```python
COST_ADJUSTMENT_R050 = {"regime_id": "R050", "version": "1.0.0", "note": "capability-gated: identity record; module always UNKNOWN on this stack"}
```

| State | Spread × | Impact × | Borrow × | Fee add (bps) | Applies when |
|---|---|---|---|---|---|
| all states | `1.0` | `1.0` | `1.0` | `0.0` | identity — module is UNKNOWN here |

**Application rule.** `identity record: R050 never adjusts costs on this stack` All multipliers `[example]` unless
tagged otherwise — calibrate per venue; the record is versioned with
`estimator_version` and every application is decision-logged (Appendix g
v1.0.0).

## §R6 Data required

| Field | Type | Granularity | Source tier | Notes |
|---|---|---|---|---|
| Direct-feed quotes | float | intraday | — | not available on this stack |
| SIP quotes | float | intraday | Tier 2 | available — insufficient alone |
| Divergence | float | — | — | unmeasurable here |

**Data rights row** (Appendix j v1.0.0): n/a — capability-gated; fixtures synthetic

## §R7 Local build (M5 Max / 128 GB)

Nothing to build: without colocated direct feeds there is no measurement. The fixture is a one-row stub asserting the gate.

## §R8 Buy vs build

Neither — gated on colocation.

## §R9 Documented evidence

| Study / source | Market & period | Metric | Before/after cost | Caveat |
|---|---|---|---|---|
| SIP vs direct-feed latency | US equities | direct feeds lead the SIP — widely understood market structure [documented-as-observation] | n/a | the structure is real; measurement here is impossible |

Selection disclosure: these are the sources surfaced by this module's research
pass (RB-list + Grok answers RB5, `2026-09-10`). Chatbot-derived
numbers are leads, never facts — see §R10.

## §R10 Source log + unverified leads

**Sources** (citable facts only):

1. CTA consolidated SIP specification (identifier — the public SIP feed)

**Chatbot source log.** Grok (grok.com, signed-in research session,
`2026-09-10`; `batches/RB5/grok-answers.md`) answered the batch
questions for R050. Operator verification found arithmetic errors in
Grok's worked examples (corrected in this module's fixtures — e.g. R041
variance decomposition and R045 cross-sectional σ recomputed by hand; this
module never copies chatbot arithmetic). Chatbot parameter bands are
illustrative estimates and are NOT promoted to documented facts.

**Unverified leads** (chatbot-only — never in evidence tables):

- Divergence magnitudes in bps — no verifiable source on this stack; not claimed

**Capability gate (R050).** This module is THIN by construction: a non-colocated, SIP-only stack cannot measure SIP-vs-direct divergence. The module is specified and tested so the gap is explicit — it permanently emits `UNKNOWN` with the reason attached, rather than fabricating a number. Escalation requires colocated direct-feed infrastructure.
---

## Appendix — AI build prompt (R050)

Copy-paste into any chat agent:

> Build module **R050 — SIP-vs-direct divergence (capability-gated)** per `notes/module-template.md`
> v1.0.0 and this file's §R0 contract.
> Cheapest path (§1.5): prototype not buildable on this stack [internal-est];
> cheapest data candidate colocated direct feeds (not available) [internal-est]; compute pattern
> `per_bar`; skip everything until colocation exists.
> Implement `detect(state, events, cfg) -> RegimeState` with the §R3 formulas,
> honoring the Lag contract (indicator causal at `t` from inputs `≤ t`; label
> gates signals only for trades at `t+1`+; backtest lag `≥ 1`
> bar(s); no ex-post period selection). Wire F1–F5 (Appendix f v1.0.0):
> invalid input → `UNKNOWN`, never interpolate. Dual-estimator agreement (F3)
> within tolerance or `UNKNOWN`. Staleness TTL `3`× cadence (F4). Emit the
> Cost-interface adjustment record with the label. Log decisions per Appendix
> g v1.0.0. Secrets via env only. Run the fixture
> `modules/fixtures/R050_tape.csv` (`# TYPE: validation-run`) against
> `modules/fixtures/R050_expected.csv` (tolerance `1e-9`
> [default]).
> **Definition of done:** `python3 -m pytest modules/tests/test_R050.py -q`
> exits `0`. Tag every numeric literal you add with the unified enum
> (Appendix i v1.0.0); put anything unverifiable in §R10 unverified leads.
