---
id: R021
kind: regime
version: 1.0.0
template_version: 1.0.0
status: draft
batch: RB3
family: regimes
provenance_tier: D
sources:
  - "Boehmer, E., Jones, C., Zhang, X. & Zhang, X. (2021). Tracking Retail Investor Activity. J. Finance 76(5): 2249–2305 (retail price impact)."
  - "O'Hara, M., Yao, C. & Ye, M. (2014). What's Not There: Odd Lots and Market Data. J. Finance 69(5): 2199–2236 (odd-lot/institutional slicing contamination)."
unverified_sources:
  []
consumes: []
regime_ids: []
correlates_with: [T-momentum, T-contrarian]
shares_data_feed_with: [R018, R013]
seed: 9021
rng: {algorithm: PCG64, lib_pin: "numpy==2.1.0"}
plot_script: scripts/plot_R021.py
test_cmd: "python3 -m pytest modules/tests/test_R021.py -q"
datasets:
  - name: TAQ odd-lot prints
    vendor: NYSE
    product: TAQ
    schema_version: "2"
    fingerprint: "sha256:REPLACE_ON_INGEST"
    vintage: "2026-09-09"
    rights: licensed
changelog:
  - "1.0.0 2026-09-10: initial module per template v1.0.0"
---

# R021 — Retail-flow dominance regime

Retail participation: odd-lot trade share `N_odd / N_all` (odd-lot *volume* share as the complement). Retail-heavy flow behaves differently — round-trip intraday, sentiment-driven, fading on red days. Provenance **[D]**.

> Tag law: every numeric literal in prose carries one of `[documented]`
> `[default]` `[example]` `[unverified]` `[internal-est]` `[measured]`
> (legend: Appendix i v1.0.0). Machine-parsed §0 data fields, section
> numbers, rule codes (F1–F5), fail-safe codes, module IDs, and
> bibliographic identifiers are exempt.

## §1 Five-minute reviewer card

| Row | Value |
|---|---|
| Module | R021 — Retail-flow dominance regime, v1.0.0 |
| Edge verdict | `PREDICTIVE-FEATURE-ONLY` — documented proxy construction (FINRA Reg SHO / TAQ odd-lot prints); a sentiment/turnover filter — odd-lot share alone is contaminated [documented] |
| After-cost | `BEFORE-COST` — retail-heavy tapes mean faster turns and more noise (Boehmer et al. 2021: retail price impact documented) |
| Build cost | Tier S · 10–20 h [internal-est] · `$1,500–3,000` loaded [internal-est] |
| Data cost | Research TAQ / FINRA: `~$200–500`/mo [unverified]; production TAQ / exchange feed [unverified] — bands indicative, verify before budgeting |
| latency_class | daily (intraday feasible) |
| risk_class | low (feature-only) |
| Capacity | N/A — flow gauge |
| Executability | Feasible: two counters off TAQ [internal-est] |
| Risk summary | Odd-lot count alone is contaminated (institutions slice orders [documented Grok RB3]); retail is not all odd-lot and odd-lot is not all retail; 'retail = dumb' is false; the 2021 episode is an outlier sample (do not calibrate to it [documented]) |
| When it dies | treating odd-lot volume as pure retail; calibrating thresholds to 2021 |
| Regime gates | retail_dominated → fade sentiment-chasing longs, shorten horizons; baseline → institutional tape (carry as modeled) |
| Emits | `regime_state_vector` `{regime_id, state, value, estimator_version, data_vintage, computed_at}` (Appendix k v1.0.0) — never orders, never intents |

## §1.5 Cheapest / least-build path

1. **Prototype hour band:** 10–20 h [internal-est] — odd-lot trade share + volume-share complement.
2. **Cheapest-source row:** min_tier = TAQ (licensed) [internal-est]; latency_class = intraday; required_fields = odd-lot flags, trade count; price_band = `~$200–500`/mo [unverified] (indicative — verify before budgeting); build_or_buy = **build the estimator, buy the feed**.
3. **Resource budget:** modest [internal-est] [internal-est]; compute pattern `per_bar`.
4. **Skip list:** retail sentiment text.
5. **DO-NOT-CUT list:** causality assertions (`assert label_ts > indicator_ts` analog, no-lookahead test); Cost interface block (§R5); F1–F5 fail-safes + module-state enum; dual-estimator agreement (F3); staleness timeout (F4); decision-log audit records; provenance tags on every numeric literal; fixtures + `test_cmd`; secrets via env only.
6. **Escalation gate:** upgrade from cheapest when any holds — sentiment overlays (add options flow).

## §R0 Module contract

### R0.1 Typed signature

```python
def detect(state: ModuleState, events: list[Event], cfg: Config) -> RegimeState:
```

`RegimeState` = `{regime_id, state, value, estimator_version, data_vintage,
computed_at, module_state}` per the §1 emits schema. `state` is the
module-owned named state vector (trailing windows, last label, vintage
tags); `events` are canonical Events/Bars (Appendix a v1.0.0), newest last.
Documented edge cases: empty events → `UNKNOWN`; invalid fields →
`UNKNOWN` (F1, never interpolate); halt/auction events → freeze per the
market-state table.

### R0.2 Config dataclass

| Parameter | Type | Default | Range | Status |
|---|---|---|---|---|
| `retail_cut` | float | `0.65` | `0.6–0.7` | default |
| `base_cut` | float | `0.5` | `0.45–0.55` | default |
| `dual_tol` | float | `0.05` | `0.02–0.1` | default |

All defaults tagged per the tag law (status `default` ⇒ `[default]`).

### R0.3 Canonical Event/Bar mapping

| TAQ | Canonical |
|---|---|
| odd-lot flag, trade count | `n_odd`, `n_all` |

### R0.4 Time contract

> Time contract: clock domain UTC, int64 ns; authoritative causality clock =
> `event_ts`; max_skew = `1` ms [default]; jitter budget = `500` µs [default];
> bar-label = open time; staleness TTL = `3`× cadence (F4); gap policy = mask,
> no interpolation; halt/auction → discard contributions across reopen.

**Timing box:** `signal@t (per_bar, America/New_York) → earliest fill @open(t+1)`

### R0.5 Lag contract (mandatory)

- **Indicator causal rule:** the indicator at time `t` is computed using only
  data with `event_ts ≤ t`. Bar-close values of bar `t` are unavailable until
  bar `t` finalizes.
- **Gating rule:** a regime label computed at `t` may gate a signal evaluated
  at `t` **only for trades executable at `t+1` or later** — never for fills
  inside bar `t`.
- **Backtest lag:** any backtest conditioning on this regime must lag the
  label by ≥ `1` bar [default] (min_lag = `1` bar [default]; the label at t uses prints ≤ t).
- **Adversary check (no-lookahead):** the test suite asserts
  `label_ts > indicator_ts` for the earliest gated trade and that appending a
  synthetic breakout bar does not move the trailing percentile used at `t`.
- **unknown_behavior:** `UNKNOWN` is restrictive — downstream halves size /
  widens stops / stands down; never treated as benign (F1).

### R0.6 Market-state table

| State | Module behavior |
|---|---|
| CONTINUOUS_TRADING | Compute normally; emit per cadence |
| HALTED | Freeze state; emit `UNKNOWN`; discard contributions across reopen |
| AUCTION | No new labels; hold last `RegimeState`; state `DEGRADED` |
| CLOSED | Emit nothing; state `OFF` |

### R0.7 Module-state enum + error taxonomy

States per Appendix k v1.0.0: `OK | DEGRADED | UNKNOWN | OFF`. Invalid input →
`UNKNOWN`, never interpolate (Appendix f v1.0.0, F1–F5).

| Failure | State | Alert |
|---|---|---|
| Missing/stale input (TTL `3`× cadence [default]) | `UNKNOWN` | page |
| Indicator value outside mathematical bounds (F2) | `UNKNOWN` | page |
| Sentinel/Verifier disagree beyond tolerance (F3) | `UNKNOWN` | page |
| `computed_at` older than `3`× cadence (F4) | `UNKNOWN` | page |
| Insufficient lookback (warm-up) | `DEGRADED` (label withheld) | log |
| Halt/auction | `UNKNOWN` / `DEGRADED` (per table) | log |
| Kill/administrative disable | `OFF` | page |
| Anything unmapped | `UNKNOWN` | page |

### R0.8 Ops tail

- **Schedule:** daily; owner: regime-sentinel on-call.
- **Health checks:** fixture replay (`test_cmd`) green on deploy; staleness
  watchdog at `3`× cadence; dual-estimator agreement monitor.
- **Alert table:** page on `UNKNOWN` > `30` s [default] or bounds violation;
  notify on `DEGRADED`; log on single dropped events.
- **Resource budget:** modest [internal-est] [internal-est].
- **Runbook stub:** `1.` confirm feed; `2.` replay fixture; `3.` if red → set
  `OFF`, page; `4.` reconcile decision log before re-arm.

### R0.9 Secrets (env-only)

`TAQ_CREDENTIALS` via environment only. No secrets in this file, fixtures, or
logs (CI secrets-grep).

### R0.10 May / must-NOT

> **May:** emit `regime_state_vector` labels and cost-interface adjustments.
> **Must-NOT:** emit orders, order intents, signals, prices, or notionals; call
> a broker; select backtest periods ex post (F5); interpolate across gaps.

## §R1 Precise definition

Retail share `f_odd = N_odd / N_all` (trade count) + odd-lot volume share complement [documented construction]. Cross-check: share via independent counter; absolute tolerance `0.05` [default].

## §R2 Exact calculation

$$f_{odd} = \frac{N_{odd}}{N_{all}} \quad\text{[documented]}$$

Worked example [documented Grok RB3]: day-10 odd-lot `210`, all trades `380` → `0.5526` ⇒ mixed/baseline (not retail-dominated). Bands [example]: `< 0.50` baseline · `0.50–0.65` mixed · `> 0.65` retail-dominated.

**Data inputs.** trade-level odd-lot flags + all-trade count (TAQ/FINRA); volume-share complement.

**Cadence.** daily.

**Threshold bands.**

| Condition | Label | Tag |
|---|---|---|
| `< 0.50` | baseline (institutional tape) | [example] |
| `0.50–0.65` | mixed | [example] |
| `> 0.65` | retail_dominated (sentiment regime) | [example] |

Bands are `[example]`; the documented content is the contamination caveats.

## §R3 Efficacy modulation

| Affected family | Direction | Mechanism |
|---|---|---|
| Momentum (short-horizon) | **amplifies** | improves when retail dominates — retail chases (Boehmer et al.: retail trades move prices) |
| Mean-reversion | **amplifies** | retail overreacts on red days (buy-the-dip flow = reversion fuel) |
| Long-horizon value | **inverts** | dies — sentiment noise dominates fundamentals over retail windows |

## §R4 Loop spec (Sentinel / Verifier / Grok-Researcher / Adversary / Gate)

- **Sentinel:** counts odd-lot prints; publishes RSV.
- **Verifier:** independent counter; tolerance `0.05` [default]; contamination disclaimer required.
- **Grok-Researcher:** proposes windows; leads only.
- **Adversary:** checks the 'odd-lot ≠ pure retail' disclaimer appears in every table; checks 2021 is not used for calibration.
- **Gate:** rejects pure-retail interpretation of odd-lot share.
- **F1–F5 instantiated:** F1 missing/stale → `UNKNOWN` (restrictive downstream);
  F2 bounds check on every emission; F3 dual-estimator agreement within the
  stated tolerance or `UNKNOWN`; F4 staleness TTL `3`× cadence → `UNKNOWN`;
  F5 no ex-post backtest-period selection without a pre-registered definition.

## §R5 Cost interface: regime state → cost-function adjustment

Retail-heavy flow shortens the profitable horizon: the adjustment scales expected alpha down and shortens the assumed holding window in retail-dominated states.

```python
def cost_adjustment(rsv, base):
    adj = dict(base)
    adj['trade_ok'] = True
    if rsv['state'] == 'retail_dominated':
        adj['alpha_mult'] = 0.75        # [example] sentiment noise
        adj['horizon_mult'] = 0.5       # [example] faster turns
    else:
        adj['alpha_mult'] = 1.0         # [default]
        adj['horizon_mult'] = 1.0       # [default]
    return adj
```

## §R6 Fixtures & acceptance

- Tape: `modules/fixtures/R021_tape.csv` — `# TYPE: validation-run`;
  `10` daily rows; day-10 `210/380 = 0.5526` (mixed — not retail-dominated); all numbers synthetic, seed `9021` [example].
- Expected: `modules/fixtures/R021_expected.csv` — per-bar indicator
  value/state, computed causally from the tape by the chapter formula.
- Tolerance: `1e-9` [default] on floats; integer fields exact.
- Acceptance tests (`modules/tests/test_R021.py`, `9` tests): `1.` fixture
  recomputes to expected; `2.` emits valid regime_state_vector; `3.`
  no-lookahead gating (`label_ts > indicator_ts`); `4.`–`8.` F1–F5 fail-safes;
  `9.` dual-estimator agreement within tolerance.
- `test_cmd`: `python3 -m pytest modules/tests/test_R021.py -q` → exits `0`.

**Where the example is optimistic:** the fixture is arithmetic illustration,
not a backtest — no fees, no latency, no corporate actions, no vendor quirks.

## §R7 Failure modes & pitfalls

| # | Trigger → Detect → Action → Recovery | check: |
|---|---|---|
| 1 | Pure-retail claim → detect: disclaimer missing → action: Gate rejects | `check: disclaimer` |
| 2 | 2021 calibration → detect: window 2020–21 only → action: Gate rejects | `check: normal window` |
| 3 | Estimators disagree → detect: count vs volume share differ > `0.05` [default] → action: `UNKNOWN` (F3) | `check: dual agreement` |
| 4 | n_odd > n_all → detect: bad counts → action: `UNKNOWN` (F1) | `check: counts valid` |

## §R8 Evidence & honesty

| Boehmer et al. (2021) [documented] | retail trades move prices | stock-day panel | price impact, not a trading rule |
| O'Hara–Yao–Ye (2014) [documented] | odd-lot contamination | tape analysis | proxies must disclaim institutional slicing |
Selection disclosure: no fake 'retail-dominated days return X%' claim is made.

## §R9 Visuals

`plot_script` (`scripts/plot_R021.py`) renders the fixture tape and the
indicator path; all plotted numbers synthetic (seed pinned in §0).

## §R10 Source log + unverified leads

**Sources** (citable facts only):

1. Boehmer, E., Jones, C., Zhang, X. & Zhang, X. (2021). Tracking Retail Investor Activity. J. Finance 76(5): 2249–2305 (retail price impact).
2. O'Hara, M., Yao, C. & Ye, M. (2014). What's Not There: Odd Lots and Market Data. J. Finance 69(5): 2199–2236 (odd-lot/institutional slicing contamination).

**Chatbot source log.** Grok (grok.com, signed in, `2026-09-10`,
batches/RB3/grok-answers.md) answered the RB question batch for
R021. Grok output is a *lead*, never a fact: formulas kept only where
they match the cited literature above; all threshold bands carry the tag of
their actual provenance.

**Unverified leads** (chatbot-only — never in evidence tables):

- (none — all Grok-supplied figures were either matched to cited sources
  or tagged `[example]`/`[unverified]` in the body.)

---

## Appendix — AI build prompt (R021)

Copy-paste into any chat agent:

> Build module **R021 — Retail-flow dominance regime** per `notes/module-template.md`
> v1.0.0 and this file's §R0 contract.
> Cheapest path (§1.5): prototype 10–20 h [internal-est]; cheapest data
> candidate TAQ (licensed) [internal-est]; compute pattern
> `per_bar`; skip sentiment.
> Implement `detect(state, events, cfg) -> RegimeState` with the §R2 formula,
> the §R0.5 lag contract (`label_ts > indicator_ts` for the earliest gated
> trade; backtest lag ≥ `1` bar [default]), and the §R5 cost-interface
> adjustment. Wire F1–F5 (Appendix f v1.0.0): invalid input → `UNKNOWN`,
> never interpolate; dual-estimator agreement (F3); staleness TTL `3`×
> cadence (F4). Log decisions per Appendix g v1.0.0. Secrets via env only.
> Run the fixture `modules/fixtures/R021_tape.csv`
> (`# TYPE: validation-run`) against
> `modules/fixtures/R021_expected.csv` (tolerance `1e-9` [default]).
> **Definition of done:** `test_cmd` exits `0`. Tag every numeric literal you
> add with the unified enum (Appendix i v1.0.0); put anything unverifiable in
> §R10 unverified leads.
