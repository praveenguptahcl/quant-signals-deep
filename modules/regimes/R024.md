---
id: R024
kind: regime
version: 1.0.0
template_version: 1.0.0
status: draft
batch: RB3
family: regimes
provenance_tier: D
sources:
  - "CME Group — Fed funds futures (ZQ) contract specifications and settlement."
unverified_sources:
  []
consumes: []
regime_ids: []
correlates_with: [T-duration, T-value]
shares_data_feed_with: [R025, R017]
seed: 9024
rng: {algorithm: PCG64, lib_pin: "numpy==2.1.0"}
plot_script: scripts/plot_R024.py
test_cmd: "python3 -m pytest modules/tests/test_R024.py -q"
datasets:
  - name: ZQ strip
    vendor: CME
    product: ZQ futures
    schema_version: "2"
    fingerprint: "sha256:REPLACE_ON_INGEST"
    vintage: "2026-09-09"
    rights: public
changelog:
  - "1.0.0 2026-09-10: initial module per template v1.0.0"
---

# R024 — Interest-rate level/direction regime

Policy-path regime from Fed-funds futures: the binary building block `P(+25bp hike) = (implied EFFR_eom − starting EFFR) / 0.25`. The rate level reprices every duration-sensitive book. Provenance **[D]**.

> Tag law: every numeric literal in prose carries one of `[documented]`
> `[default]` `[example]` `[unverified]` `[internal-est]` `[measured]`
> (legend: Appendix i v1.0.0). Machine-parsed §0 data fields, section
> numbers, rule codes (F1–F5), fail-safe codes, module IDs, and
> bibliographic identifiers are exempt.

## §1 Five-minute reviewer card

| Row | Value |
|---|---|
| Module | R024 — Interest-rate level/direction regime, v1.0.0 |
| Edge verdict | `PREDICTIVE-FEATURE-ONLY` — documented market arithmetic (ZQ strip); a duration/discount overlay — compute from the strip, never scrape FedWatch [documented] |
| After-cost | `BEFORE-COST` — rates move discount rates and funding simultaneously — the regime gates duration, not direction |
| Build cost | Tier S · 10–20 h [internal-est] · `$1,500–3,000` loaded [internal-est] |
| Data cost | Research CME ZQ / FRED: free [documented]; production CME data / FRED [unverified] — bands indicative, verify before budgeting |
| latency_class | daily (intraday on FOMC days) |
| risk_class | low (feature-only) |
| Capacity | N/A — macro gauge |
| Executability | Feasible: ZQ strip → implied EFFR [internal-est] |
| Risk summary | FedWatch is a display product (compute from the strip [documented Grok RB3]); the binary block ignores meeting-day weighting (use the full strip for production); pricing ≠ realized policy (the regime is about expectations, not outcomes) |
| When it dies | scraping FedWatch probabilities into a model (terms + staleness); treating one meeting's binary as the whole path |
| Regime gates | hike_biased → duration underweight; cut_biased → duration overweight |
| Emits | `regime_state_vector` `{regime_id, state, value, estimator_version, data_vintage, computed_at}` (Appendix k v1.0.0) — never orders, never intents |

## §1.5 Cheapest / least-build path

1. **Prototype hour band:** 10–20 h [internal-est] — ZQ-strip hike probability + 3-obs cross-check.
2. **Cheapest-source row:** min_tier = CME/FRED (free) [internal-est]; latency_class = daily; required_fields = ZQ strip, starting EFFR; price_band = free [unverified] (indicative — verify before budgeting); build_or_buy = **build the estimator, buy the feed**.
3. **Resource budget:** negligible [internal-est] [internal-est]; compute pattern `per_bar`.
4. **Skip list:** full meeting-day weighting (prototype).
5. **DO-NOT-CUT list:** causality assertions (`assert label_ts > indicator_ts` analog, no-lookahead test); Cost interface block (§R5); F1–F5 fail-safes + module-state enum; dual-estimator agreement (F3); staleness timeout (F4); decision-log audit records; provenance tags on every numeric literal; fixtures + `test_cmd`; secrets via env only.
6. **Escalation gate:** upgrade from cheapest when any holds — policy-path trading (add day weights).

## §R0 Module contract

### R0.1 Typed signature

```python
def detect(state: ModuleState, events: list[Event], cfg: Config) -> RegimeState:
```

`RegimeState` = `{regime_id, state, value, estimator_version, data_vintage,
computed_at, module_state}` per the §1 emits schema. `state` is the
module-owned named state vector (trailing windows, last label, vintage
tags); `events` are canonical Events/Bars (Appendix a v1.0.0), newest last.
Documented edge cases: empty events → `UNKNOWN`; invalid fields →
`UNKNOWN` (F1, never interpolate); halt/auction events → freeze per the
market-state table.

### R0.2 Config dataclass

| Parameter | Type | Default | Range | Status |
|---|---|---|---|---|
| `hike_p` | float | `0.6` | `0.5–0.7` | default |
| `cut_p` | float | `-0.2` | `-0.3–-0.1` | default |
| `dual_tol` | float | `0.15` | `0.1–0.25` | default |

All defaults tagged per the tag law (status `default` ⇒ `[default]`).

### R0.3 Canonical Event/Bar mapping

| CME ZQ | Canonical |
|---|---|
| implied EOM EFFR, starting EFFR | `effr_implied`/`effr_start` |

### R0.4 Time contract

> Time contract: clock domain UTC, int64 ns; authoritative causality clock =
> `event_ts`; max_skew = `1` ms [default]; jitter budget = `500` µs [default];
> bar-label = open time; staleness TTL = `3`× cadence (F4); gap policy = mask,
> no interpolation; halt/auction → discard contributions across reopen.

**Timing box:** `signal@t (per_bar, America/New_York) → earliest fill @open(t+1)`

### R0.5 Lag contract (mandatory)

- **Indicator causal rule:** the indicator at time `t` is computed using only
  data with `event_ts ≤ t`. Bar-close values of bar `t` are unavailable until
  bar `t` finalizes.
- **Gating rule:** a regime label computed at `t` may gate a signal evaluated
  at `t` **only for trades executable at `t+1` or later** — never for fills
  inside bar `t`.
- **Backtest lag:** any backtest conditioning on this regime must lag the
  label by ≥ `1` bar [default] (min_lag = `1` bar [default]; strip as of t; the label is an expectation, not a realized policy).
- **Adversary check (no-lookahead):** the test suite asserts
  `label_ts > indicator_ts` for the earliest gated trade and that appending a
  synthetic breakout bar does not move the trailing percentile used at `t`.
- **unknown_behavior:** `UNKNOWN` is restrictive — downstream halves size /
  widens stops / stands down; never treated as benign (F1).

### R0.6 Market-state table

| State | Module behavior |
|---|---|
| CONTINUOUS_TRADING | Compute normally; emit per cadence |
| HALTED | Freeze state; emit `UNKNOWN`; discard contributions across reopen |
| AUCTION | No new labels; hold last `RegimeState`; state `DEGRADED` |
| CLOSED | Emit nothing; state `OFF` |

### R0.7 Module-state enum + error taxonomy

States per Appendix k v1.0.0: `OK | DEGRADED | UNKNOWN | OFF`. Invalid input →
`UNKNOWN`, never interpolate (Appendix f v1.0.0, F1–F5).

| Failure | State | Alert |
|---|---|---|
| Missing/stale input (TTL `3`× cadence [default]) | `UNKNOWN` | page |
| Indicator value outside mathematical bounds (F2) | `UNKNOWN` | page |
| Sentinel/Verifier disagree beyond tolerance (F3) | `UNKNOWN` | page |
| `computed_at` older than `3`× cadence (F4) | `UNKNOWN` | page |
| Insufficient lookback (warm-up) | `DEGRADED` (label withheld) | log |
| Halt/auction | `UNKNOWN` / `DEGRADED` (per table) | log |
| Kill/administrative disable | `OFF` | page |
| Anything unmapped | `UNKNOWN` | page |

### R0.8 Ops tail

- **Schedule:** daily; owner: regime-sentinel on-call.
- **Health checks:** fixture replay (`test_cmd`) green on deploy; staleness
  watchdog at `3`× cadence; dual-estimator agreement monitor.
- **Alert table:** page on `UNKNOWN` > `30` s [default] or bounds violation;
  notify on `DEGRADED`; log on single dropped events.
- **Resource budget:** negligible [internal-est] [internal-est].
- **Runbook stub:** `1.` confirm feed; `2.` replay fixture; `3.` if red → set
  `OFF`, page; `4.` reconcile decision log before re-arm.

### R0.9 Secrets (env-only)

`CME_API_KEY` via environment only. No secrets in this file, fixtures, or
logs (CI secrets-grep).

### R0.10 May / must-NOT

> **May:** emit `regime_state_vector` labels and cost-interface adjustments.
> **Must-NOT:** emit orders, order intents, signals, prices, or notionals; call
> a broker; select backtest periods ex post (F5); interpolate across gaps.

## §R1 Precise definition

P(+25bp) = (implied EFFR_eom − starting EFFR)/0.25 [documented construction]. Cross-check: 3-obs average implied path; absolute tolerance `0.15` [default]. Compute from the ZQ strip — never scrape FedWatch [documented].

## §R2 Exact calculation

$$P(+25bp) = \frac{\text{implied EFFR}_{eom} - \text{starting EFFR}}{0.25} \quad\text{[documented]}$$

Worked example [documented Grok RB3]: implied `4.50%`, starting `4.33%` → `(0.17)/0.25 = 0.68 = 68%` ⇒ hike_biased. Bands [example]: `P > 0.6` hike_biased · `−0.2 ≤ P ≤ 0.6` on_hold · `P < −0.2` cut_biased.

**Data inputs.** ZQ futures strip → implied end-of-month EFFR; starting EFFR (NY Fed).

**Cadence.** daily.

**Threshold bands.**

| Condition | Label | Tag |
|---|---|---|
| `P > 0.6` | hike_biased (duration underweight) | [example] |
| `−0.2 ≤ P ≤ 0.6` | on_hold | [example] |
| `P < −0.2` | cut_biased (duration overweight) | [example] |

Bands are `[example]`; the documented content is the strip arithmetic.

## §R3 Efficacy modulation

| Affected family | Direction | Mechanism |
|---|---|---|
| Duration / bond proxies | **amplifies** | improves — the regime *is* the duration view |
| Value (long-duration equities) | **amplifies** | cut_biased supports long-duration value; hike_biased inverts it |
| Carry / funding trades | **inverts** | dies when the path reprices funding (R017 cross-check) |

## §R4 Loop spec (Sentinel / Verifier / Grok-Researcher / Adversary / Gate)

- **Sentinel:** pulls the ZQ strip; publishes RSV.
- **Verifier:** 3-obs path cross-check; meeting-day weighting in production.
- **Grok-Researcher:** proposes windows; leads only.
- **Adversary:** checks no FedWatch scraping; checks the full path is available.
- **Gate:** rejects scraped probabilities.
- **F1–F5 instantiated:** F1 missing/stale → `UNKNOWN` (restrictive downstream);
  F2 bounds check on every emission; F3 dual-estimator agreement within the
  stated tolerance or `UNKNOWN`; F4 staleness TTL `3`× cadence → `UNKNOWN`;
  F5 no ex-post backtest-period selection without a pre-registered definition.

## §R5 Cost interface: regime state → cost-function adjustment

The policy path reprices duration: the adjustment tilts duration exposure with the hike/cut bias — a discount-rate overlay, not a direction bet.

```python
def cost_adjustment(rsv, base):
    adj = dict(base)
    adj['trade_ok'] = True
    if rsv['state'] == 'hike_biased':
        adj['duration_mult'] = 0.5    # [example] underweight duration
    elif rsv['state'] == 'cut_biased':
        adj['duration_mult'] = 1.5    # [example] overweight duration
    else:
        adj['duration_mult'] = 1.0    # [default]
    return adj
```

## §R6 Fixtures & acceptance

- Tape: `modules/fixtures/R024_tape.csv` — `# TYPE: validation-run`;
  `10` daily rows of (starting EFFR, implied EOM); final `P = 68%` → hike_biased; all numbers synthetic, seed `9024` [example].
- Expected: `modules/fixtures/R024_expected.csv` — per-bar indicator
  value/state, computed causally from the tape by the chapter formula.
- Tolerance: `1e-9` [default] on floats; integer fields exact.
- Acceptance tests (`modules/tests/test_R024.py`, `9` tests): `1.` fixture
  recomputes to expected; `2.` emits valid regime_state_vector; `3.`
  no-lookahead gating (`label_ts > indicator_ts`); `4.`–`8.` F1–F5 fail-safes;
  `9.` dual-estimator agreement within tolerance.
- `test_cmd`: `python3 -m pytest modules/tests/test_R024.py -q` → exits `0`.

**Where the example is optimistic:** the fixture is arithmetic illustration,
not a backtest — no fees, no latency, no corporate actions, no vendor quirks.

## §R7 Failure modes & pitfalls

| # | Trigger → Detect → Action → Recovery | check: |
|---|---|---|
| 1 | FedWatch scrape → detect: scraped probabilities → action: Gate rejects | `check: from strip` |
| 2 | One meeting as path → detect: single binary only → action: require full strip in prod | `check: full path` |
| 3 | Expectation as outcome → detect: 'rates will hike' claim → action: relabel expectation | `check: expectations` |
| 4 | Estimators disagree → detect: |p − p̄3| > `0.15` [default] → action: `UNKNOWN` (F3) | `check: dual agreement` |

## §R8 Evidence & honesty

| CME [documented] | ZQ contract specs and settlement | n/a (market structure) | the arithmetic is a market identity |
Selection disclosure: the binary block is documented as ignoring meeting-day weighting [documented Grok RB3].

## §R9 Visuals

`plot_script` (`scripts/plot_R024.py`) renders the fixture tape and the
indicator path; all plotted numbers synthetic (seed pinned in §0).

## §R10 Source log + unverified leads

**Sources** (citable facts only):

1. CME Group — Fed funds futures (ZQ) contract specifications and settlement.

**Chatbot source log.** Grok (grok.com, signed in, `2026-09-10`,
batches/RB3/grok-answers.md) answered the RB question batch for
R024. Grok output is a *lead*, never a fact: formulas kept only where
they match the cited literature above; all threshold bands carry the tag of
their actual provenance.

**Unverified leads** (chatbot-only — never in evidence tables):

- (none — all Grok-supplied figures were either matched to cited sources
  or tagged `[example]`/`[unverified]` in the body.)

---

## Appendix — AI build prompt (R024)

Copy-paste into any chat agent:

> Build module **R024 — Interest-rate level/direction regime** per `notes/module-template.md`
> v1.0.0 and this file's §R0 contract.
> Cheapest path (§1.5): prototype 10–20 h [internal-est]; cheapest data
> candidate CME/FRED (free) [internal-est]; compute pattern
> `per_bar`; skip day weights.
> Implement `detect(state, events, cfg) -> RegimeState` with the §R2 formula,
> the §R0.5 lag contract (`label_ts > indicator_ts` for the earliest gated
> trade; backtest lag ≥ `1` bar [default]), and the §R5 cost-interface
> adjustment. Wire F1–F5 (Appendix f v1.0.0): invalid input → `UNKNOWN`,
> never interpolate; dual-estimator agreement (F3); staleness TTL `3`×
> cadence (F4). Log decisions per Appendix g v1.0.0. Secrets via env only.
> Run the fixture `modules/fixtures/R024_tape.csv`
> (`# TYPE: validation-run`) against
> `modules/fixtures/R024_expected.csv` (tolerance `1e-9` [default]).
> **Definition of done:** `test_cmd` exits `0`. Tag every numeric literal you
> add with the unified enum (Appendix i v1.0.0); put anything unverifiable in
> §R10 unverified leads.
