---
id: R032
kind: regime
version: 1.0.0
template_version: 1.0.0
status: draft
batch: RB4
family: regimes
provenance_tier: D
sources:
  - "WealthManagement / Amundi reviews of US stock-bond correlation history — post-2000 average ≈ -0.2, 1926-1999 average +0.18 (cited via Grok; treated as [unverified] here)"
unverified_sources:
  - Baur & Lucey flight-to-quality formalization — cited by Grok; not independently verified
  - "Baele et al. FTS-days (<`5`% [unverified] of sample) — cited by Grok; not independently verified"
  - Grok RB4 worked example ρ = +0.35 — illustrative input
consumes: []
regime_ids: []
correlates_with:
  - "T: risk-parity / balanced strategies"
  - "T: CTA / trend strategies"
shares_data_feed_with:
  - R031 (cross-asset correlation)
  - R024 (rates path)
seed: 42032
rng: {algorithm: PCG64, lib_pin: "numpy==2.1.0"}
plot_script: scripts/plot_R032.py
test_cmd: "python3 -m pytest modules/tests/test_R032.py -q"
datasets:
  - name: SPY+TLT EOD
    vendor: Norgate Data
    product: US Stocks Platinum
    schema_version: 1
    fingerprint: "sha256:REPLACE_ON_INGEST"
    vintage: 2026-09-09
    rights: licensed
changelog:
  - "1.0.0 2026-09-10: initial module per template v1.0.0"
---

# R032 — Flight-to-quality (stock/bond correlation sign)

Sign of the 63-day [example] stock–bond return correlation (SPY vs TLT): negative = normal hedge; positive = inflation-fear regime. Provenance **[D]**.

> Tag law: every numeric literal in prose carries one of `[documented]
> [default] [example] [unverified] [internal-est] [measured]` (legend: Appendix
> i v1.0.0). Constants inside cited indicator formulas inherit the formula's
> tag. Bibliographic years, volumes, pages, regulation numbers, section
> numbers, fail-safe codes (F1-F5), module IDs, clock times, and machine-parsed
> `§0` data fields are identifiers, not literals, and are exempt.

## §1 Five-minute reviewer card

| Row | Value |
|---|---|
| Module | R032 — Flight-to-quality (stock/bond correlation sign), v1.0.0 |
| Edge verdict | `PREDICTIVE-FEATURE-ONLY` — documented conditioning power over S/T efficacy; no standalone after-cost edge claimed |
| After-cost | `BEFORE-COST` — a regime is a conditioner, not a traded signal; one-sentence verdict: when ρ flips positive, risk-parity dies (the hedge becomes a second loss) while CTA/trend improves on cleaner macro trends. |
| Build cost | Tier S · `10–20` h [internal-est] · `$1,500–3,000` loaded [internal-est] |
| Data cost | Research `$50–80`/mo (Norgate indicative) [unverified] — verify before budgeting |
| latency_class | daily |
| risk_class | low (regime-only; no order placement) |
| Capacity | not applicable — the module emits state only; capacity is set by the gated S/T modules |
| Executability | Feasible: `~40` lines [internal-est] Python; one rolling correlation |
| Risk summary | Sign flips on noise without the 21-day confirmation — whipsaw is the failure mode |
| When it dies | TLT/IEF substitution changes the sign; vendor total-return revision |
| Regime gates | R031 vs R032 can disagree: 2022 was high cross-asset corr + positive stock/bond corr (inflation regime), not FTQ |
| Emits | `regime_state_vector` (Appendix b v1.0.0 adapted) `{regime_id, state, value, estimator_version, data_vintage, computed_at}` — never orders |

## §1.5 Cheapest / least-build path

1. **Prototype hour band:** `10–20` h [internal-est] — SPY/TLT rolling correlation + 21-day sign confirmation + fixture.
2. **Cheapest-source row:** min_tier = Norgate EOD (or Massive EOD) — cheapest data source
   candidate [internal-est]; latency_class = daily;
   required_fields = SPY and TLT daily total returns; price_band = `$50–80`/mo
   [unverified] (indicative — verify before budgeting); build_or_buy =
   **build the estimator; buy the EOD series**.
3. **Resource budget:** `1` core, `<1` GB RAM [internal-est]; compute pattern
   `per_bar`.
4. **Skip list:** strict FTQ flag with vol filter (phase 2); IEF substitution as live verifier.
5. **DO-NOT-CUT list:** causality assertions (`assert fill_event > signal_event`, no-lookahead
   test); COST block + callable `expected_cost_bps`; F1-F5 fail-safes +
   module-state enum; decision-log audit records; provenance tags on every
   numeric literal; fixtures + `test_cmd`; secrets via env only.
6. **Escalation gate:** upgrade from cheapest when any holds — needing the strict Baele-style FTQ flag with the vol filter.

## §R0 Module contract

### R0.1 Typed signature

```python
def detect(state: RegimeModuleState, events: list[Event], cfg: Config) -> RegimeState:
```

`RegimeState` = `{regime_id, state, value, estimator_version, data_vintage,
computed_at, module_state, min_lag_bars}` — the module's `regime_state_vector`
(§1). `state` is the module-owned named state vector (`rho`, `module_state`);
`events` are canonical Events (Appendix a v1.0.0), ascending `event_ts`. The
function handles documented edge cases: empty events → `UNKNOWN`;
out-of-bounds indicator values → `UNKNOWN` (F2, never interpolate); halt/auction
events → freeze per the market-state table. It emits regime labels and
cost-interface adjustments only — never orders, never signals.

### R0.2 Config dataclass

| Parameter | Type | Default | Range | Status |
|---|---|---|---|---|
| `W` | int | `63` | `20–126` | example |
| `confirm_days` | int | `21` | `10–42` | example |
| `pos` | float | `0.20` | `0.10–0.30` | example |
| `min_lag_bars` | int | `1` | `1–5` | default |

All defaults tagged per the tag law (status `default` ⇒ `[default]`).

### R0.3 Canonical schema mapping

Vendor columns → canonical Event schema (Appendix a v1.0.0), stated once here:

| Norgate SPY/TLT (`date`, total return) | canonical pair series |
| IEF (second source) | verifier substitution |

### R0.4 Time contract

> Time contract: clock domain UTC, int64 ns; authoritative causality clock =
> `event_ts`; max_skew = `1` ms [default]; bar-label = open time; staleness TTL
> = `3`× cadence [default] (F4); gap policy = mask, no interpolation; halt →
> freeze and emit `UNKNOWN`.

**Timing box:** `detect@t (per_bar, America/New_York) → regime label gates signals only for trades @open(t+1) or later`

### Lag contract

- The indicator is causal: the value/state published for time `t` is computed
  using only inputs with timestamps `≤ t` (Appendix e v1.0.0).
- A regime label finalized at `t` may gate signals only for trades at `t+1`
  or later — `earliest fill @open(t+1)` per the bar-label rule.
- Backtests: regime-conditioned variants lag the label by `≥ min_lag_bars` =
  `1` bar(s) [default]; no ex-post period selection (F5).
- No-lookahead check: for every prefix of the input series, the label at `t`
  is identical with and without later data (enforced in `test_no_lookahead`).

### R0.5 Market-state table

| State | Module behavior |
|---|---|
| CONTINUOUS_TRADING | Compute normally; emit per cadence |
| HALTED | Freeze state; emit `UNKNOWN`; discard contributions across reopen |
| AUCTION | No new labels; hold last state; `DEGRADED` |
| CLOSED | Emit nothing; state `OFF` |

### R0.6 Module-state enum + error taxonomy

States per Appendix k v1.0.0: `OK | DEGRADED | UNKNOWN | OFF`. Invalid input →
`UNKNOWN`, never interpolate (Appendix f v1.0.0, F1–F5).

| Failure | State | Alert |
|---|---|---|
| Missing SPY/TLT print (F1) | `UNKNOWN` | page |
| Non-finite correlation (F2) | `UNKNOWN` | page |
| IEF-substituted sign disagrees (F3) | `UNKNOWN` | page |
| Series stale > `3` days [example] (F4) | `UNKNOWN` | notify |
| Kill/administrative disable | `OFF` | page |
| Anything unmapped | `UNKNOWN` | page |

### R0.7 Ops tail

- **Schedule:** daily after EOD (`~18:00` ET [example]); owner: regime-plant on-call.
- **Health checks:** fixture replay (`test_cmd`) green on deploy; dual-estimator
  agreement monitor (F3); staleness watchdog at `3`× cadence (F4).
- **Alert table:** page on `UNKNOWN` > `30` s [default] or bounds violation;
  notify on `DEGRADED`; log on single dropped inputs.
- **Resource budget:** `1` core, `<1` GB RAM [internal-est].
- **Runbook stub:** `1.` confirm feed; `2.` replay fixture; `3.` if red → set
  `OFF`, page; `4.` reconcile decision log before re-arm.

### R0.8 Secrets (env-only)

(none — licensed EOD via local DB) via environment only. No secrets in this file, fixtures, or
logs (CI secrets-grep).

### R0.9 May / must-NOT

> **May:** emit regime labels (`RegimeState`) and cost-interface adjustments
> per the Cost interface block. **Must-NOT:** emit orders, order intents,
> signals, prices, or notionals; select backtest periods ex post (F5);
> interpolate across gaps; call a broker; mutate shared state outside `state`.

### R0.10 Cost-interface hook

The typed adjustment record lives in the **Cost interface** block below; it is
the only channel by which this regime touches cost inputs.

## §R1 Legacy (subsumed by §1)

Legacy §R1 content is the §1 reviewer card above; this header is retained so
section numbering stays frozen.

## §R2 Precise definition

Sign of the 63-day [example] stock–bond return correlation (SPY vs TLT): negative = normal (bonds hedge equities); positive = inflation-fear regime (both sell off together — 2022 printed sustained positive, the 60/40 portfolio's worst year [documented-as-observation]). Sign flips require 21-day [example] confirmation — no whipsaw on noise.

## §R3 Exact calculation

Rolling Pearson `ρ_t` over window `W = 63` trading days [example] on daily
total returns (decimal):

$$\rho_t = \frac{\mathrm{Cov}(r_e, r_b)}{\sigma_e \sigma_b}}$$

Strict FTQ flag (illustrative operationalization):
`FTQ_t = 1[ρ_t < ρ_FTQ] · 1[σ_e,t > σ_e,crit] · 1[r_e,t < 0 < r_b,t]`.
Worked check (8-day panel, illustrative [example]): `ρ ≈ 0.3312` →
INFLATION_FEAR (positive; not a flight-to-quality day).

*Formula tag: [example] — constants inside this formula inherit it per the tag law
(Appendix i v1.0.0); worked-check numbers carry their own tags.*

**Data inputs.** SPY and TLT (or local equity index + 10y total-return) daily series; window W.

**Cadence.** Daily.

**Threshold bands.**

| `ρ_63d` | State | Tag |
|---|---|---|
| `< −0.2` | normal hedge | [example] |
| `−0.2…+0.2` | transition | [example] |
| `> +0.2` | inflation-fear | [example] |

Recalibrate every `[example]`-tagged band per instrument before production use.

## §R4 Efficacy modulation

Direction ∈ `amplifies | degrades | inverts`; mechanism states *why*.

| Affected S/T families | Direction | Mechanism |
|---|---|---|
| T: risk-parity / balanced strategies | degrades | the hedge becomes a second loss — diversification ratio collapses |
| T: CTA / trend | amplifies | trends are cleaner when the macro driver is singular |

## §R5 Loop spec

| Role | Responsibility |
|---|---|
| Sentinel | computes ρ(SPY, TLT) daily |
| Verifier | substitutes IEF for TLT (second estimator); sign disagreement → UNKNOWN |
| Grok-Researcher | tracks stock/bond correlation literature — leads only |
| Adversary | injects a 1-day sign flip; the 21-day confirmation must hold the label |
| Gate | accepts only with the confirmation rule documented |

**F1–F5 instantiated for this regime.**

- **F1.** missing SPY/TLT print → `UNKNOWN`
- **F2.** non-finite correlation → `UNKNOWN` + alert
- **F3.** IEF substitution must agree on sign or `UNKNOWN`
- **F4.** series stale > `3` days [example] → `UNKNOWN`
- **F5.** 21-day confirmation fixed ex ante; no flip-picking

## Cost interface: regime state → cost-function adjustment

The regime state maps to an adjustment record applied to the strategy's
`expected_cost_bps(notional, adv_pct, venue, side, urgency)` inputs. The record
is the *only* cost channel (no cost numbers are restated elsewhere).

```python
COST_ADJUSTMENT_R032 = {"regime_id": "R032", "version": "1.0.0", "note": "tilt-only regime; no direct cost adjustment", "INFLATION_FEAR": {"spread_mult": 1.0, "impact_mult": 1.0, "borrow_mult": 1.0, "fee_add_bps": 0.0, "tag": "[default]"}}
```

| State | Spread × | Impact × | Borrow × | Fee add (bps) | Applies when |
|---|---|---|---|---|---|
| all states | `1.0` | `1.0` | `1.0` | `0.0` | identity — tilt only |

**Application rule.** `identity record: R032 gates allocation tilts, never cost inputs` All multipliers `[example]` unless
tagged otherwise — calibrate per venue; the record is versioned with
`estimator_version` and every application is decision-logged (Appendix g
v1.0.0).

## §R6 Data required

| Field | Type | Granularity | Source tier | Notes |
|---|---|---|---|---|
| SPY total return | float | daily | Tier 2 | Norgate |
| TLT total return | float | daily | Tier 2 | Norgate |
| Window W | int | static | — | `63` days [example] |

**Data rights row** (Appendix j v1.0.0): Norgate licensed; redistribution restricted; fixtures synthetic

## §R7 Local build (M5 Max / 128 GB)

One rolling correlation. The fixture uses an 8-day panel [example]; production W = `63` days [example].

## §R8 Buy vs build

Build the estimator; buy the EOD series.

## §R9 Documented evidence

| Study / source | Market & period | Metric | Before/after cost | Caveat |
|---|---|---|---|---|
| US stock-bond correlation history (post-2000 avg ≈ −0.2; 1926–1999 avg +0.18) | US, 1926–2022 | long-run sign regimes [unverified] | n/a | secondary citation via chatbot — do not over-claim |
| 2022 sustained positive stock/bond correlation | US, 2022 | 60/40 worst year [documented-as-observation] | BEFORE-COST | observation, not a rule |

Selection disclosure: these are the sources surfaced by this module's research
pass (RB-list + Grok answers RB4, `2026-09-10`). Chatbot-derived
numbers are leads, never facts — see §R10.

## §R10 Source log + unverified leads

**Sources** (citable facts only):

1. WealthManagement / Amundi reviews of US stock-bond correlation history — post-2000 average ≈ -0.2, 1926-1999 average +0.18 (cited via Grok; treated as [unverified] here)

**Chatbot source log.** Grok (grok.com, signed-in research session,
`2026-09-10`; `batches/RB4/grok-answers.md`) answered the batch
questions for R032. Operator verification found arithmetic errors in
Grok's worked examples (corrected in this module's fixtures — e.g. R041
variance decomposition and R045 cross-sectional σ recomputed by hand; this
module never copies chatbot arithmetic). Chatbot parameter bands are
illustrative estimates and are NOT promoted to documented facts.

**Unverified leads** (chatbot-only — never in evidence tables):

- Grok RB4: Baur & Lucey FTQ formalization — not independently verified
- Grok RB4: Baele et al. FTS days <`5`% [unverified] of sample — not independently verified

---

## Appendix — AI build prompt (R032)

Copy-paste into any chat agent:

> Build module **R032 — Flight-to-quality (stock/bond correlation sign)** per `notes/module-template.md`
> v1.0.0 and this file's §R0 contract.
> Cheapest path (§1.5): prototype `10–20` h [internal-est];
> cheapest data candidate Norgate EOD (or Massive EOD) [internal-est]; compute pattern
> `per_bar`; skip the strict vol-filtered FTQ flag.
> Implement `detect(state, events, cfg) -> RegimeState` with the §R3 formulas,
> honoring the Lag contract (indicator causal at `t` from inputs `≤ t`; label
> gates signals only for trades at `t+1`+; backtest lag `≥ 1`
> bar(s); no ex-post period selection). Wire F1–F5 (Appendix f v1.0.0):
> invalid input → `UNKNOWN`, never interpolate. Dual-estimator agreement (F3)
> within tolerance or `UNKNOWN`. Staleness TTL `3`× cadence (F4). Emit the
> Cost-interface adjustment record with the label. Log decisions per Appendix
> g v1.0.0. Secrets via env only. Run the fixture
> `modules/fixtures/R032_tape.csv` (`# TYPE: validation-run`) against
> `modules/fixtures/R032_expected.csv` (tolerance `1e-9`
> [default]).
> **Definition of done:** `python3 -m pytest modules/tests/test_R032.py -q`
> exits `0`. Tag every numeric literal you add with the unified enum
> (Appendix i v1.0.0); put anything unverifiable in §R10 unverified leads.
